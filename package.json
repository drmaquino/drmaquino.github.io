{
  "dependencies": {
    "bootstrap": "^5.3.1",
    "sweetalert2": "^11.10.6"
  },
  "devDependencies": {
    "@types/node": "^20.11.30"
  },
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "test:lcov": "node --test --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/lcov.info"
  }
}